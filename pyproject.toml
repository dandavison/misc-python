[project]
name = "misc-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "ipython",
    "rich",
    "textual",
    "typer",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
